# app/

Streamlit UI. Nothing built yet -- Week 3 starts the guided single-stub
form. This package must never contain payroll calculations of its own;
it calls into `engine/` and renders what comes back. See
[`docs/architecture.md`](../docs/architecture.md).
